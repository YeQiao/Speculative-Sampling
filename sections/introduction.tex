% Write 4-5 paragraphs:
% 1) Why decoding latency matters for practical LLM use.
% 2) Why speculative decoding is promising but brittle in practice.
% 3) Why tiny SSM drafters are attractive for efficiency.
% 4) Contributions list (method + implementation insight + empirical evidence + reproducibility).
% 5) Brief roadmap of paper sections.

% Suggested contribution bullets:
% - We propose SpecSSM, a speculative decoding setup using a tiny SSM drafter paired with a larger verifier.
% - We identify and evaluate implementation details that materially affect accepted-token dynamics and throughput.
% - We provide broad empirical evaluation across checkpoints/configurations and report speed-quality trade-offs.
